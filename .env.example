# Root env for docker compose. Copy to .env.
POSTGRES_USER=pipeline
POSTGRES_PASSWORD=pipeline
POSTGRES_DB=nyc311

# AI layer (mock needs no keys)
AI_PROVIDER=mock
AI_MODEL=mock-1
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

JWT_SECRET=change-me-in-prod
